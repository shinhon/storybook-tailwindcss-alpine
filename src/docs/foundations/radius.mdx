import { Meta } from "@storybook/addon-docs/blocks";

import radii from "../../tokens/generated/radius.json";
import { RadiusView } from "./_views/RadiusView";

<Meta title="Foundations/Radius" />

# Radius

Radius tokens shape controls, surfaces, and containers across the Design System. Select a CSS variable to copy it.

<RadiusView tokens={radii} />
